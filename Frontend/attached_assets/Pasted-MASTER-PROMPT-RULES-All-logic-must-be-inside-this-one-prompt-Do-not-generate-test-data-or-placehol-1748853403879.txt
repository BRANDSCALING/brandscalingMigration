MASTER PROMPT RULES: All logic must be inside this one prompt. Do not generate test data or placeholders. Do not use children props. Do not change the routing structure. Fix all scrolling issues. Fix mobile sidebar toggle. Keep sidebar fixed on desktop. Keep admin header sticky. Only the content area should scroll. Must work for all pages (Dashboard, Users, Posts, etc). Do not assume use of additional libraries. Fix layout at a structural level.

TASK: Rebuild the layout of the admin panel at `client/src/pages/admin/index.tsx` and `components/AdminSidebar.tsx` to solve the following issues:
- Scroll only works partially (doesn’t reach bottom)
- Sidebar breaks on mobile
- Header not consistently sticky
- Layout conflicts with routing logic
- Previous patches introduced nested overflow bugs

🔧 Step 1: In `client/src/pages/admin/index.tsx`, replace entire file with:

```tsx
import React, { useState } from "react";
import AdminSidebar from "./components/AdminSidebar";
import AdminHeader from "./components/AdminHeader";
import { useLocation } from "wouter";
import Dashboard from "./dashboard";
import Users from "./users";
import Posts from "./posts";
import Tags from "./tags";
import Moderation from "./moderation";
import Insights from "./insights";

export default function AdminPanel() {
  const [sidebarOpen, setSidebarOpen] = useState(false);
  const [location] = useLocation();

  const renderPage = () => {
    switch (location) {
      case "/admin/dashboard":
        return <Dashboard />;
      case "/admin/users":
        return <Users />;
      case "/admin/posts":
        return <Posts />;
      case "/admin/tags":
        return <Tags />;
      case "/admin/moderation":
        return <Moderation />;
      case "/admin/insights":
        return <Insights />;
      default:
        return <Dashboard />;
    }
  };

  return (
    <div className="h-screen w-screen flex overflow-hidden">
      {/* Sidebar */}
      <AdminSidebar sidebarOpen={sidebarOpen} setSidebarOpen={setSidebarOpen} />

      {/* Main content */}
      <div className="flex-1 flex flex-col h-screen overflow-hidden">
        {/* Sticky Header */}
        <div className="sticky top-0 z-50">
          <AdminHeader setSidebarOpen={setSidebarOpen} />
        </div>

        {/* Scrollable content */}
        <div className="flex-1 overflow-y-auto p-6 bg-gray-50">
          {renderPage()}
        </div>
      </div>
    </div>
  );
}